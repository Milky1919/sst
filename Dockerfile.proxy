FROM python:3.10-slim

RUN pip install --no-cache-dir fastapi uvicorn httpx python-multipart

COPY sbv2_proxy.py /app/sbv2_proxy.py

WORKDIR /app

EXPOSE 5933

CMD ["uvicorn", "sbv2_proxy:app", "--host", "0.0.0.0", "--port", "5933"]
