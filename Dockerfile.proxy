FROM python:3.10-slim

RUN pip install --no-cache-dir fastapi uvicorn httpx

COPY sbv2-proxy.py /app/sbv2-proxy.py

EXPOSE 5933

CMD ["uvicorn", "sbv2-proxy:app", "--host", "0.0.0.0", "--port", "5933"]
